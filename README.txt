RELAY TETRIS - TERMUX SETUP

1. Install packages:

pkg update -y
pkg install nodejs -y

2. Install websocket package:

npm install ws

3. Run server:

node server.js

4. Find your IP:

ip addr

5. Open in browser:

http://YOUR_IP:3000

Both phones must be on same Wi-Fi.
